# Projects

This directory holds individual presentation projects created by `project_manager.py`.

Each project is a self-contained workspace with its own sources, SVG output, and exports.

## Quick Start

```bash
python3 skills/ppt-master/scripts/project_manager.py init <project_name> --format ppt169
```
